# AIO + NVMe-oF/RDMA 实验流程

本文记录基于 `dd731_with_index`、SPDK `nvmf_tgt`、AIO 后端和实时解包程序的端到端实验流程。

## 1. 实验架构

```text
发送端 dd731
    ↓ /dev/nvmeXn1
NVMe-oF / RDMA
    ↓
接收端 nvmf_tgt
    ↓
AIO bdev
    ↓
payload_stream.bin
    ↓
进度监控与实时解包
    ↓
restored_live/
```

发送端将远端 namespace 当作普通块设备使用；接收端使用普通文件作为 SPDK AIO 后端。本实验不需要将 NVMe 设备绑定到用户态。

## 2. 参数配置


| 参数 | 占位符 | 说明 |
|---|---|---|
| 接收端地址 | `<TARGET_IP>` | 接收端 RDMA 地址，示例：`<TARGET_IP>` |
| RDMA 端口 | `<RDMA_PORT>` | 示例：`<RDMA_PORT>` |
| 测试 NQN | `<TEST_NQN>` | 不要直接提交真实 NQN |
| 测试 serial | `<TEST_SERIAL>` | 不要直接提交真实 serial |
| AIO bdev | `Aio0` | SPDK bdev 名称 |
| AIO 文件 | `/path/payload_stream.bin` | 可按环境调整 |
| index 文件 | `/path/<INDEX_NAME>.index` | 替换 `<INDEX_NAME>` |
| 发送 namespace | `/dev/<NAMESPACE_NAME>` | 替换 `<NAMESPACE_NAME>` |

## 3. 接收端配置

### 3.1 启动 SPDK


```bash
cd ~/spdk
./build/bin/nvmf_tgt -c config.json
```

### 3.2 创建 AIO bdev


```bash
truncate -s 1G /path/to/payload_stream.bin
cd ~/spdk/scripts
./rpc.py bdev_aio_create \
  /root/spdk/payload_stream.bin \
  Aio0 \
  512
```
1G后端大小可调整，此处仅示例

**状态确认命令**

```bash
./rpc.py bdev_get_bdevs
```

确认输出中出现 `Aio0`。`1G` 是后端逻辑容量，不代表实际一定占用 1G；稀疏文件通常只占用已写入的数据空间。

AIO 后端容量应满足：

```text
AIO 容量 ≥ index total_size × 1.2
```

计算结果向上取整，例如 `1G`、`2G`、`16G`、`32G` 等。
示例：约 10 MB 的 payload 使用 1G AIO；约 10 GB 的 payload 使用 16G AIO；约 100 GB 的 payload 使用 128G 或更大 AIO。

### 3.3 创建 subsystem、namespace 和 RDMA listener

**输入命令**

```bash
./rpc.py nvmf_create_subsystem \
  <TEST_NQN> \
  -a -s <TEST_SERIAL> \
  -d "Pipeline Test Target"

./rpc.py nvmf_subsystem_add_ns \
  <TEST_NQN> Aio0

./rpc.py nvmf_subsystem_add_listener \
  <TEST_NQN> \
  -t rdma -a <TARGET_IP> -s <RDMA_PORT>
```

**状态确认命令**

```bash
./rpc.py bdev_get_bdevs
./rpc.py nvmf_get_subsystems
```

确认 `Aio0` 已创建，并确认 subsystem、namespace 与 RDMA listener 均已出现。

## 4. 发送端连接

**输入命令**

```bash
sudo nvme connect \
  -t rdma \
  -n <TEST_NQN> \
  -a <TARGET_IP> -s <RDMA_PORT>
```

**状态确认命令**

```bash
sudo nvme list
sudo nvme list-subsys
```

确认已找到本次测试对应的 `/dev/nvmeXn1`。该设备路径只会出现在发送端。

## 5. 接收端启动实时恢复

将 `live_unpack_payload_stream.py` 和 `monitor_aio_progress.py` 复制到接收端，例如 `/root/spdk/`。启动顺序如下。

### 5.1 启动 Aio0 进度监控

**输入命令**

```bash
python3 monitor_aio_progress.py \
  --rpc /root/spdk/scripts/rpc.py \
  --bdev Aio0 \
  --total <TOTAL_SIZE_BYTES> \
  --progress /root/spdk/payload.progress \
  --interval 0.5
```

监控器读取 Aio0 的 `bytes_written`，并以启动时累计值为基线，避免将旧测试数据计入本次进度。

### 5.2 启动实时解包

**输入命令**

```bash
python3 live_unpack_payload_stream.py \
  /root/spdk/<INDEX_NAME>.index \
  /root/spdk/payload_stream.bin \
  /root/spdk/restored_live \
  /root/spdk/payload.progress \
  --overwrite
```

程序只恢复已经完整到达的 index 条目，并使用 `.live-part` 临时文件，避免留下半截文件。

## 6. 发送端执行 dd731_with_index

**输入命令**

```bash
./dd731_with_index \
  -m 0x2 \
  --idir /root/spdk/input_files \
  --index /root/spdk/<INDEX_NAME>.index \
  --bs 65536 \
  --adaptive 1 \
  --of /dev/<NAMESPACE_NAME>
```

将 `/dev/<NAMESPACE_NAME>` 替换为发送端实际 namespace。`--of` 用于绕过 `dd731` 对测试 serial 的自动识别。
-m 参数为指定CPU核，此处为核2，可更换；
--idir 参数指定输入文件目录，此处为 `input_files`;
--index 输入索引文件，此处为 `<INDEX_NAME>.index`;
-bs 65536 块大小为 64 KB，可更换；
--adaptive 1 启用自适应块大小，可关闭；
--of 输入目标文件，此处为 `/dev/<NAMESPACE_NAME>`

## 7. 成功验收标准

实验满足以下条件时，可认为闭环验证成功：

- 发送端已通过 RDMA 连接测试 NQN。
- 接收端 `Aio0` 的 `bytes_written` 持续增长。
- 发送尚未结束时，`/root/spdk/restored_live` 已出现恢复文件。
- 实时解包最终报告恢复完成，例如 `completed: <N> files restored`。
- 进度文件显示 `written_bytes=<TOTAL_SIZE_BYTES>/<TOTAL_SIZE_BYTES>` 且 `done=1`。
- `raw_delta` 可能因底层 512 字节块对齐而略大于逻辑总长度，这是正常现象。

## 8. 关键注意事项

1. AIO 容量不是 payload 长度；`1G` 只是后端块设备的逻辑容量。
2. Payload 总长度应按 512 字节对齐。若总长度未对齐，最后一次 I/O 可能失败。
3. 不要手动修改 `payload.progress` 中的 `done` 字段。
4. AIO 正在使用时，不要重新执行 `truncate` 修改后端文件大小。
5. 若只恢复了部分文件，应依次检查 `payload.progress`、index 尾部条目以及 `dd731` 的完整错误日志。
6. 本实验不需要在接收端创建 `Nvme0`，也不需要执行用户态 NVMe 绑定；已有原生 NVMe 实验配置应保持不变。

## 9. 数据流总结

```text
dd731
  → NVMe-oF/RDMA
  → nvmf_tgt
  → Aio0
  → payload_stream.bin
  → 进度监控
  → 实时解包
  → restored_live/
```
